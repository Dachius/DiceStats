A website for statistical analysis of dice.

Generally supports explicit combinatorics-derived calculations, with precision up to the precision of javascript floating point numbers. Uses Monte Carlo approximation when no tractable mathematical solution is available.

Uses ChartJS for chart rendering.